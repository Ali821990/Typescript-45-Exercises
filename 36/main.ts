function make_Shirt (size: string, printMessage: string) {
    console.log (`You have selected ${size} size shirt with ${printMessage} prints on shirt`)
}

make_Shirt("Large", "Gorverner's IT Initiative")

